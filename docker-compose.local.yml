version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: profitzen
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dev_password_123
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_SHARED_PRELOAD_LIBRARIES: pg_stat_statements
    ports:
      - "5433:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
      - ./database/init-schemas.sql:/docker-entrypoint-initdb.d/01-init-schemas.sql
    networks:
      - profitzen-network

  redis:
    image: redis:7-alpine
    command: redis-server --maxmemory 128mb --maxmemory-policy allkeys-lru
    ports:
      - "6379:6379"
    networks:
      - profitzen-network

  # identity-service:
  #   build:
  #     context: ./backend
  #     dockerfile: src/Services/Identity/Dockerfile.dev
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Development
  #     - ASPNETCORE_URLS=http://+:80
  #     - ConnectionStrings__DefaultConnection=Host=postgres;Database=profitzen;Username=identity_user;Password=dev_password_123;Schema=identity
  #   volumes:
  #     - ./backend/src/Services/Identity:/app/src/Services/Identity
  #     - ./backend/src/Shared:/app/src/Shared
  #   depends_on:
  #     - postgres
  #   networks:
  #     - profitzen-network

  # ollama:
  #   image: ollama/ollama:latest
  #   restart: always
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_local_data:/root/.ollama
  #   networks:
  #     - profitzen-network

  # ollama-pull-model:
  #   image: curlimages/curl
  #   container_name: profitzen-ollama-puller
  #   depends_on:
  #     - ollama
  #   networks:
  #     - profitzen-network
  #   command: >
  #     sh -c "sleep 10 && curl -X POST http://ollama:11434/api/pull -d '{\"name\": \"llama3\"}'"

  seq:
    image: datalust/seq:latest
    container_name: profitzen-seq
    environment:
      - ACCEPT_EULA=Y
      - SEQ_FIRSTRUN_ADMINPASSWORD=admin_password_123
    ports:
      - "8081:80"
      - "5341:5341"
    networks:
      - profitzen-network
    volumes:
      - seq_data:/data

volumes:
  postgres_dev_data:
  ollama_local_data:
  seq_data:


networks:
  profitzen-network:
    driver: bridge
